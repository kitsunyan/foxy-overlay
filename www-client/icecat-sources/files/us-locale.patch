--- a/makeicecat
+++ b/makeicecat
@@ -58,6 +58,7 @@

 mkdir l10n
 cd l10n
+echo en-US > ../$SOURCEDIR/browser/locales/shipped-locales
 while read line;do
     line=$(echo $line |cut -d' ' -f1)
     #[ $line = "es-ES" ] || continue # To speed up testing
